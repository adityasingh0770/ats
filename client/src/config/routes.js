/** Public marketing / chapter landing URL (used when hosting as the main entry). */
export const HOME_PATH = '/mensuration-grade-8';

/** Merge portal redirect entry point. */
export const CHAPTER_PATH = '/chapter';
