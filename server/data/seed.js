/**
 * Legacy MongoDB seed script — no longer used.
 * Questions load from ./questions.seed.js and content from ./content.seed.json at runtime.
 */
console.log('No database seed needed: questions and content are bundled as static files.');
process.exit(0);
